/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
const isMatch = (s, p) => {
    return recursiveIsMatch(0, 0, s, p)
}
const recursiveIsMatch = (i, j, str, pat) => {
    
}
